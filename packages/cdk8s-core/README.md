# cdk8s-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cdk8s-core` to build the library.

## Running unit tests

Run `nx test cdk8s-core` to execute the unit tests via [Jest](https://jestjs.io).
