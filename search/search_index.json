{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"cdk8s Catalog","text":"<p>A catalog of reusable, Helm-like cdk8s constructs for Kubernetes. Every library in the catalog is a small, focused cdk8s chart \u2014 one npm package per application (<code>@cdk-x/metric-server</code>, <code>@cdk-x/argocd</code>, ...) \u2014 built on a shared foundation (<code>@cdk-x/cdk8s-core</code>) so they all look and behave the same way.</p>"},{"location":"#why-a-catalog","title":"Why a catalog","text":"<ul> <li>One package per app. Each library deploys exactly one application   (metrics-server, ArgoCD, ...) with sensible defaults, not a generic   \"Kubernetes resources\" toolkit.</li> <li>Helm-like customization. Every construct exposes grouped props with   defaults, fluent <code>addXxx()</code> methods for collections, and an   always-available escape hatch (<code>apiObject.addJsonPatch(...)</code>) for   anything not modeled.</li> <li>Consistent labeling. Every resource gets the Kubernetes   recommended common labels   plus catalog-specific <code>cdk8s-catalog/library-name</code>/<code>-version</code> labels, for   free, via <code>CatalogChart</code>.</li> <li>jsii, so pick your language. Every library publishes to npm, PyPI,   Maven, NuGet, and Go \u2014 use it from TypeScript, Python, Java, C#, or Go.</li> </ul>"},{"location":"#where-to-start","title":"Where to start","text":"<ul> <li>Libraries</li> </ul> <p>The published libraries themselves \u2014 what they deploy, how to install and   use them, and their full API reference in your language of choice.</p> <ul> <li>Building a library</li> </ul> <p>Contributing a new library to the catalog? The pattern every library   follows (folder layout, construct design, testing).</p>"},{"location":"#versioning","title":"Versioning","text":"<p>This catalog-level site (Home, Building a library, Libraries index) is evergreen \u2014 it always reflects <code>main</code>. Each library has its own versioned docs site instead (version selector in its own header), since a library's major version tracks the Kubernetes generation it targets (e.g. <code>1.x.x</code> for Kubernetes 1.34, <code>2.x.x</code> for 1.35, ...) and libraries release independently of each other.</p>"},{"location":"building-a-library/","title":"Building a library","text":"<p>This is the pattern every cdk8s library in this catalog follows, built on top of <code>CatalogChart</code>. The <code>@cdk-x/metric-server</code> package is the canonical reference implementation \u2014 read it alongside this guide.</p>"},{"location":"building-a-library/#scaffolding-a-new-library","title":"Scaffolding a new library","text":"<p><code>sh pnpm nx g @cdk-x/nx-plugin:cdk8s-library &lt;library&gt; [--description=\"...\"] [--keywords=a,b]</code></p> <p>This generator (<code>tools/nx-plugin</code>) scaffolds the whole pattern in one shot: the base TS library (via <code>@nx/js:library</code>), the jsii setup (<code>.jsii</code> targets, <code>tsconfig.jsii.json</code>, the <code>jsii-*</code> <code>project.json</code> targets), the library's own mkdocs site (<code>mkdocs.yml</code>, <code>docs/index.md</code>, the <code>docs-build</code>/<code>docs-serve</code>/ <code>docs-deploy</code> targets \u2014 see Docs site below), the <code>@cdk-x/cdk8s-core</code> + <code>cdk8s</code> + <code>cdk8s-plus-&lt;N&gt;</code> dependencies (pinned to whatever <code>metric-server</code> currently declares, so it keeps working across a Kubernetes version bump \u2014 see Kubernetes version below \u2014 without any change to the generator itself), and registers the library in Libraries.</p> <p>It only scaffolds the bare skeleton: <code>src/index.ts</code> plus an empty <code>&lt;Library&gt;</code> <code>Chart</code> extending <code>CatalogChart</code>. Add each <code>src/&lt;kind&gt;/</code> folder by hand afterwards, following the pattern below. The generator targets application-deploying libraries \u2014 i.e. everything except <code>cdk8s-core</code> itself, which has no <code>cdk8s-plus</code> dependency and no per-kind folders.</p>"},{"location":"building-a-library/#principles","title":"Principles","text":"<ol> <li>One folder per Kubernetes <code>kind</code>. Each folder under <code>src/</code> owns a    single resource kind, wrapped in a focused construct, with a colocated    unit test.</li> <li>OOP only. Everything is a <code>class</code>. No exported functions.</li> <li>Helm-like customization. Sensible defaults, overridable via props,    extensible after construction \u2014 plus an always-available escape hatch.</li> <li>Compose in a <code>Chart</code>. A root chart (extending    <code>CatalogChart</code>)    wires the per-kind constructs together.</li> <li>Test every kind. Targeted assertions per construct + one chart    snapshot.</li> </ol> <p>Kubernetes version</p> <p>The catalog currently targets Kubernetes 1.34 \u2192 every library uses <code>cdk8s-plus-34</code>. Each library's own docs site is versioned by its own semver, not by this Kubernetes axis directly \u2014 a major version bump is how a library signals it now targets a new Kubernetes generation.</p>"},{"location":"building-a-library/#directory-layout","title":"Directory layout","text":"<p><code>text packages/&lt;library&gt;/src/   index.ts                     # public API surface   &lt;library&gt;.ts                 # the Chart (composition root, extends CatalogChart)   &lt;library&gt;.spec.ts            # full-synth snapshot test   &lt;kind&gt;/                      # one folder per kind     &lt;kind&gt;.ts                  #   the construct     &lt;kind&gt;.spec.ts             #   its unit test     index.ts                   #   re-export   ...</code></p> <p><code>metric-server</code> example: <code>deployment/</code>, <code>service/</code>, <code>service-account/</code>, <code>api-service/</code>, <code>cluster-role/</code>, <code>cluster-role-binding/</code>, <code>role-binding/</code>. RBAC is split strictly per kind; the chart instantiates the parameterized role/binding constructs and wires their cross-references.</p>"},{"location":"building-a-library/#construct-design","title":"Construct design","text":"<ul> <li>Reusable resources extend <code>Construct</code>; the composition root extends   <code>CatalogChart</code>.</li> <li>Every construct takes a <code>MyThingProps</code> interface. Each optional field   carries a JSDoc <code>@default</code>. Resolve with <code>props.x ?? DEFAULT</code>.</li> <li>Expose the underlying cdk8s-plus object as a <code>public readonly</code> field \u2014 it   is the escape hatch (see below) and lets the chart wire things together.</li> <li>ESM/nodenext: all intra-package imports use the <code>.js</code> extension   (<code>import { Foo } from './foo/index.js'</code>).</li> </ul>"},{"location":"building-a-library/#customization-surface-layered-the-cdk-l2l1-model","title":"Customization surface \u2014 layered (the CDK L2/L1 model)","text":"<p>Don't try to model every Kubernetes field in props. Model the common ~80% and provide escape hatches for the rest:</p> <ol> <li>Grouped props + defaults for common knobs (<code>image</code>, <code>replicas</code>,    <code>imagePullPolicy</code>, <code>strategy</code>, <code>resources</code>, <code>probes</code>). Nest related    options (e.g. <code>probes?: { liveness?; readiness?; startup? }</code>) instead of    one flat interface.</li> <li>Fluent <code>addXxx()/setXxx(): this</code> only for collections built    incrementally (args, env, volumes, RBAC rules/subjects). Scalars stay    props-only \u2014 don't add a setter for every field.</li> <li>Escape hatch, always available. Expose the underlying object and use    <code>obj.apiObject.addJsonPatch(JsonPatch.replace('/spec/...', value))</code> for    the long tail you don't model.</li> </ol>"},{"location":"building-a-library/#enums-for-domain-values","title":"Enums for domain values","text":"<p>Reuse cdk8s-plus's own enums (<code>ImagePullPolicy</code>, <code>DeploymentStrategy</code>, <code>Protocol</code>, <code>SeccompProfileType</code>, <code>ConnectionScheme</code>, <code>Capability</code>, \u2026). Author your own string enum only for domain-specific values \u2014 e.g. known CLI flag keys:</p> <p><code>ts export enum MetricsServerArg {   CERT_DIR = 'cert-dir',   SECURE_PORT = 'secure-port',   // ... }</code></p> <p>Keep it extensible: type the method as <code>addArg(key: string, value: string)</code>, not <code>addArg(key: MetricsServerArg, ...)</code>, so callers get autocomplete via the enum but can still pass a flag you didn't enumerate.</p>"},{"location":"building-a-library/#deferred-rendering-how-addxxx-reaches-the-manifest","title":"Deferred rendering (how <code>addXxx</code> reaches the manifest)","text":"<p>cdk8s-plus higher constructs wrap their whole spec in <code>Lazy.any({ produce: () =&gt; this._toKube() })</code>, so the manifest is rendered at synth, not at construction. That is what makes post-construction mutation work. Two facts to internalize:</p> <ul> <li><code>ApiObject.toJson()</code> runs <code>resolve()</code> (which resolves <code>Lazy</code>) before   applying <code>JsonPatch</code>. So a <code>JsonPatch</code> value cannot itself carry a <code>Lazy</code>.</li> <li>cdk8s core only exposes <code>Lazy.any(producer)</code> (returns <code>any</code>) \u2014 there is no   <code>Lazy.list</code>/<code>Lazy.string</code>.</li> </ul> <p>Recommended recipe for a mutable collection (args), as in <code>deployment.ts</code>:</p> <ul> <li>Hold the state in a <code>Map&lt;string,string&gt;</code> (single source of truth; dedups   by key, so <code>addArgs</code> overrides a default rather than duplicating a   flag).</li> <li>Hand cdk8s-plus a live array reference and re-sync it from the Map on   each mutation. Do not pass <code>Lazy.any(...)</code> as the whole <code>args</code> value:   <code>Container.get args()</code> does <code>[...this._args]</code>, which throws on a   non-iterable <code>Lazy</code>.</li> </ul> <p>```ts private readonly args = new Map(/ defaults /); private readonly renderedArgs: string[] = [];   // handed to the container <p>public addArgs(args: Record): this {   for (const [k, v] of Object.entries(args)) this.args.set(k, v);   this.syncArgs();   return this; } private syncArgs(): void {   this.renderedArgs.length = 0;   this.renderedArgs.push(...this.renderArgs()); } private renderArgs(): string[] {   return [...this.args].map(([k, v]) =&gt; (v === '' ? <code>--${k}</code> : <code>--${k}=${v}</code>)); } ``` <p>Fallback if a cdk8s-plus field copies/validates the value at construction (so a live reference doesn't survive): apply concrete values via <code>obj.apiObject.addJsonPatch(JsonPatch.replace(path, this.renderArgs()))</code>.</p>"},{"location":"building-a-library/#testing","title":"Testing","text":"<p>Use cdk8s <code>Testing</code> with Jest (SWC transform is already configured):</p> <ul> <li>Per-kind spec: <code>Testing.chart()</code> \u2192 instantiate the construct \u2192   <code>Testing.synth(chart)</code> \u2192 targeted assertions on the synthesized   manifest (ports, image, verbs, apiGroups, \u2026). Include a test that calls   <code>addXxx</code> after construction and asserts the change lands at synth.</li> <li>Chart spec: one <code>expect(Testing.synth(chart)).toMatchSnapshot()</code> over the   whole composition.</li> </ul>"},{"location":"building-a-library/#commands","title":"Commands","text":"<p><code>sh pnpm nx build &lt;library&gt;    # tsc build -&gt; dist/ pnpm nx test  &lt;library&gt;    # jest (per-kind specs + chart snapshot) pnpm nx lint  &lt;library&gt;    # eslint, incl. @nx/dependency-checks</code></p>"},{"location":"building-a-library/#docs-site","title":"Docs site","text":"<p>Every library gets its own versioned docs site (mirror <code>packages/metric-server/</code>). The generator already creates all of this \u2014 the breakdown below is for understanding the pieces, and for <code>cdk8s-core</code>, the one library it doesn't scaffold:</p> <ol> <li><code>packages/&lt;library&gt;/mkdocs.yml</code> (<code>INHERIT: ../../mkdocs.yml</code>, own <code>site_name</code>/    <code>site_url</code>/<code>edit_uri</code>, <code>extra.version.provider: mike</code>, and a <code>nav:</code> starting    with <code>- Home: https://cdk-x.github.io/cdk8s-catalog/</code> \u2014 each library's site    is a separate mkdocs build with no other way back to the catalog root \u2014 then    <code>index.md</code>).</li> <li><code>packages/&lt;library&gt;/docs/index.md</code> \u2014 the intro (install command, usage    example, what it creates). Include <code>_compat.md</code> and <code>_lang-switcher.md</code> near    the top: <code>--8&lt;-- \"packages/&lt;library&gt;/docs/_compat.md\"</code> /    <code>--8&lt;-- \"packages/&lt;library&gt;/docs/_lang-switcher.md\"</code> \u2014 both are generated by    <code>tools/docs-prepare-library.mjs</code>, don't write them by hand.</li> <li><code>docs-build</code>/<code>docs-serve</code>/<code>docs-deploy</code> targets in <code>project.json</code> (copy    metric-server's or cdk8s-core's verbatim, swapping the library name).</li> <li>If the library has a <code>jsii-docs</code> target, <code>tools/docs-prepare-library.mjs</code>    automatically turns its per-language <code>dist-jsii/docs/API.&lt;lang&gt;.md</code> output    into <code>docs/api/&lt;lang&gt;.md</code> pages with a language-icon switcher between them    (see <code>packages/metric-server/mkdocs.yml</code>'s <code>API Reference</code> nav section for    the pattern to copy) \u2014 no manual step needed beyond adding that nav section.</li> </ol> <p>Then add the library to Libraries \u2014 already done for you if you used the generator. Docs tooling (mkdocs, mkdocs-material, mike) is pinned in <code>docs/requirements.txt</code> \u2014 install it with <code>pip install -r docs/requirements.txt</code> before running <code>nx docs-serve</code>/ <code>docs-build</code>/<code>docs-deploy</code>, so your local versions match CI.</p>"},{"location":"libraries/","title":"Libraries","text":"<p>Each library is an independently versioned npm package that deploys one application onto Kubernetes, built on <code>CatalogChart</code>. Each library has its own docs site with its own version selector \u2014 a library's major version tracks the Kubernetes generation it targets (e.g. <code>1.x.x</code> for Kubernetes 1.34, <code>2.x.x</code> for 1.35, ...).</p> Library Package Deploys cdk8s Core <code>@cdk-x/cdk8s-core</code> Shared foundation every library builds on \u2014 not deployed on its own Metric Server <code>@cdk-x/metric-server</code> metrics-server <p>New to the catalog? See Building a library for the pattern every library follows.</p>"}]}